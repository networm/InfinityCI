import { TaskDashboard } from "@/components/task-dashboard";

/** Home dashboard: tasks by default (Jenkins-style rows, favorites pinned). */
export function DashboardPage() {
  return <TaskDashboard />;
}
