# 固定依赖版本 + README 技术栈文档

## 1. 固定所有版本

**npm（web/package.json）**：
- 将全部 dependencies / devDependencies 从 `^` 范围改为**实际安装的精确版本**（从 package-lock.json 读取解析版本，例如 `"react": "^19.2.8"` → `"19.2.8"`）
- 增补 `engines` 字段声明 Node/npm 要求（Node ≥ 20，npm ≥ 10）
- 修改后 `npm install` 刷新 lockfile 并验证构建 + typecheck 通过

**.NET（csproj）**：已是精确版本（如 2.83.0、10.0.12，无范围符号），且 SDK 由 global.json 固定（10.0.401）——无需改动，README 中说明即可。

**提交**：`chore(web): pin frontend dependencies to exact versions`

## 2. README 技术栈章节

在 `## 架构` 章节后新增 `## 技术栈与依赖`（内容用上一步固定后的精确版本）：

- **运行环境要求**表：.NET SDK 10.0.401（global.json 固定）、Node.js ≥ 20、npm ≥ 10、Git（Webhook clone/配置仓库依赖）
- **.NET NuGet 包**：按 Core / Server / Agent / 测试 四组表格列出全部包与精确版本（Google.Protobuf 3.36.1、Grpc.* 2.83.0、LibGit2Sharp 0.32.0、YamlDotNet 18.1.0、EF Core Sqlite 10.0.12、Serilog.AspNetCore 10.0.0、xunit 2.9.3 等）
- **前端依赖**：运行时（react 19.2.8、@tanstack/*、@microsoft/signalr、@xterm/xterm、zustand、js-yaml、i18next 等）与开发时（Rsbuild 2、TypeScript、Tailwind CSS 4、@types/*）两张表，全部精确版本
- **构建与发布**：Rsbuild（dev 端口 3000、/api 与 /hubs 代理）、`scripts/publish.cmd`（win-x64 自包含单文件）

**提交**：`docs(readme): document full tech stack and pinned dependency versions`

## 验证
- `npm run typecheck && npm run build` 通过
- `dotnet test` 全量回归（确认无意外改动）