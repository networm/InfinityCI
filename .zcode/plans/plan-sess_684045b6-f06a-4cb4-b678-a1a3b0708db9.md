# 博客文章写作计划：《只用 Z Code，9 天写出一个 CI 服务器》

## 已完成调研

**CI 软件（Infinity CI）**：90 个提交（2026-09-10 ~ 09-18），净 +22,212 行。第一天凌晨 00:22–02:01 约 2 小时 9 个提交搭出 .NET 10 后端 + React 19 前端 + gRPC Agent 完整闭环，当天上午重构为 GitHub Actions 式模型；之后每天一个主题迭代（DAG 调度/配置 Git 仓库/SCM/LDAP/实时化/Docker 等）。类 Jenkins/GHA 的自托管 CI：YAML 工作流 + 并行 DAG 调度 + SignalR 实时 + xterm.js 彩色日志 + 拉取式分布式 Agent + RBAC/LDAP + 五渠道通知。

**Unity 工程（20260917-UnityCIGame）**：Unity 6（6000.0.23f1）。9 月 17 日上午 07:59 建工程 → 08:12 加入 `CI/platform/windows/` 三脚本（prepare.py 打印环境变量自检、build.py 调 Unity 命令行 `Builder.BuildWindows`、deploy.py 占位）→ 09:01 首次 CI 打包产出 `Builds/Windows/UnityCIGame.exe`（约 95MB）→ 09:06 收尾。**从建工程到 CI 跑通约 1 小时**。build.py 里有真实踩坑痕迹：`-logFile -` + Popen 实时转发 Unity 日志、「退出码 0 但产物不存在」双重校验、`UNITY_PATH` 环境变量覆盖 Hub 默认路径；09:05 还把日志方案从"失败后打尾部 50 行"升级为实时输出。

**时间线亮点**：9 月 17 日是「决战日」——CI 侧凌晨在打磨 xterm 日志终端体验，上午就在新工程上跑通了 Unity 6 Windows 打包全流程，两侧互相印证。

## 实施步骤

### 第 1 步：提取 ZCode session 真实对话历史（用户已授权）

- 用 Python `sqlite3` 以 `mode=ro` 只读打开 `~/.zcode/cli/db/db.sqlite`（66MB），查表结构，定位 session/消息表。
- 按工作目录与时间（09-09 ~ 09-18）筛选该项目的 session，输出清单（id、时间、首条用户消息摘要）。
- 精读关键 session，摘录：① 第一天的初始需求提示词；② 典型交互片段（提需求→实现→验证）；③ Unity 打包跑通相关对话。素材存临时笔记。
- Session 过大时只取用户消息 + 助手关键结论；若数据库中查不到旧 session，则基于 git 历史合理还原，并在交付时说明。

### 第 2 步：撰写博客（输出 `C:\Users\NETWORM\Work\Projects\20260909-Infinity-CI\blog.md`）

中文，4000–6000 字，软件介绍与开发过程并重，第一人称。大纲：

1. **引言**：为什么自研 CI——Unity 独立开发者的打包痛点；Jenkins 太重、GitHub Actions 在云端进不了内网。
2. **Infinity CI 是什么**：定位（单 EXE 的 Jenkins / 内网里的 GitHub Actions）、功能全景、技术栈（.NET 10 + React 19 + gRPC + SignalR + xterm.js）、架构示意。
3. **第一天：2 小时，从 mkdir 到能用的 CI**：凌晨 0:22–2:01 时间线还原 + 真实提示词引用；上午两记重构升级为 GHA 式并行 Run 模型。
4. **迭代九天**：第二天功能大爆炸（23 提交：DAG、配置即 Git 仓库、webhook、企业微信通知）→ 企业化（LDAP/i18n/暗色主题）→ 任务优先数据布局 → 实时化冲刺 + 六大功能补齐（104 测试全绿）→ 生态集成（GitHub/GitLab/Gitea、五渠道通知、Docker）→ xterm 体验打磨。附每日提交分布。
5. **决战 9·17：Unity 6 打包 Windows 跑通**：讲两侧真实细节——Unity 工程侧 `Builder.BuildWindows`（场景取自 Build Settings、StandaloneWindows64）+ build.py 的 Unity 命令行调用原文、实时日志转发、产物双重校验；CI 侧只需一个 YAML 工作流调三个 Python 脚本（附 workflow.yml）；「CI 无一行 Unity 代码」的编排哲学；95MB 产物跑通。1 小时从建工程到出包。
6. **用 Z Code 写整个软件的体会**：小步提交即验证、测试护航、放心让 AI 大重构（breaking 重构随手做）、人做产品决策 AI 做实现、一款工具全栈通吃（后端/前端/DevOps/Python/C# 全包）。
7. **结语**：9 天 90 提交 +22,212 行，项目数据汇总 + 开源地址。

穿插：真实对话引用块、git 数据、YAML/Python/C# 代码片段、截图占位符（供你后续补图）。

### 第 3 步：核对交付

- 核对文中事实（时间线、提交数据、Unity 版本、功能点）与两个 git 仓库/README/对话历史一致；
- 标注哪些是合理演绎、哪些有真实对话佐证；
- 交付 blog.md，汇报结构与字数。